name: MLB Results Update

on:
  schedule:
    # 09:00 UTC = 5am ET during EST, 6am ET during EDT
    # Late enough for extra-inning West Coast games, early enough that
    # downstream MLB Games Update (11 UTC) sees fresh outcomes.
    - cron: '0 9 * * *'
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Single date override (YYYY-MM-DD). Leave blank for yesterday.'
        required: false
        default: ''
      backfill_days:
        description: 'Rolling backfill: re-run yesterday + last N days. Use 0 for none.'
        required: false
        default: '0'
      backfill_start:
        description: 'Manual range start (YYYYMMDD). Pair with backfill_end.'
        required: false
        default: ''
      backfill_end:
        description: 'Manual range end (YYYYMMDD). Pair with backfill_start.'
        required: false
        default: ''

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: mlb_results_update
        run: pip install -r requirements.txt

      - name: Run mlb_results_update
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TARGET_DATE: ${{ github.event.inputs.target_date || '' }}
          BACKFILL_DAYS: ${{ github.event.inputs.backfill_days || '0' }}
          BACKFILL_START: ${{ github.event.inputs.backfill_start || '' }}
          BACKFILL_END: ${{ github.event.inputs.backfill_end || '' }}
          LOG_LEVEL: INFO
        run: python -m mlb_results_update
